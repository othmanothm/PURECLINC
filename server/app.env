PORT=5000
CLIENT_ORIGIN=http://localhost:5173
BASE_URL=http://localhost:5000

DATABASE_URL=mysql+pymysql://root:root@localhost:3306/pureskin_clinic

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=pureskin_clinic

JWT_SECRET=super_secret_jwt_key_change_me
JWT_EXPIRES_IN=1h

STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# SMTP — SMTP_HOST is the MAIL SERVER hostname (not your email address).
# Google / Google Workspace: smtp.gmail.com | Microsoft 365: smtp.office365.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=mohammed.ismail2@students.alquds.edu
SMTP_PASS=fehpbwauhedzglix

MAIL_FROM="PureSkin Clinic <mohammed.ismail2@students.alquds.edu>"
EMAIL_VERIFICATION_EXPIRY_MINUTES=15
EMAIL_VERIFICATION_RESEND_SECONDS=60

# Appointment reminder email (confirmed bookings only). Cron: every 15 min by default.
# APPOINTMENT_REMINDER_ENABLED=false
# APPOINTMENT_REMINDER_HOURS_BEFORE=24
# APPOINTMENT_REMINDER_WINDOW_MINUTES=30
# APPOINTMENT_REMINDER_CRON=*/15 * * * *